apiVersion: v1
kind: Service
metadata:
  name: redis-service # This name matches the 'redis-service' in your app manifest
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379